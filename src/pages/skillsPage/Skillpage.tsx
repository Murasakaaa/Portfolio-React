interface SkillData {
  title: string;
  description: string;
  skills: string[];
}

interface SkillPageProps {
  skillData: SkillData;
}

export default function SkillPage({ skillData }: SkillPageProps) {
  return (
    <main>
      {/* À compléter avec ton contenu */}
    </main>
  );
}